# Инструкция по сборке APK для тестирования

## Подготовка

1. Убедитесь что у вас установлен EAS CLI:
```bash
npm install -g eas-cli
```

2. Войдите в Expo аккаунт:
```bash
eas login
```

## Сборка APK для тестирования

### Вариант 1: Preview билд (Рекомендуется для тестирования)

Этот билд быстрее собирается и сразу создаёт APK файл:

```bash
cd c:/dev/EnglishPlatform
eas build --profile preview --platform android
```

**Что произойдет:**
- ✅ Соберётся APK (не AAB)
- ✅ API URL будет `http://212.67.11.39:3002/api`
- ✅ Билд соберётся на серверах Expo (~10-15 минут)
- ✅ Получите ссылку для скачивания APK
- ✅ Можно установить на любой Android телефон

### Вариант 2: Development билд (Для разработки с hot reload)

Этот билд включает development client и позволяет подключаться к Metro:

```bash
cd c:/dev/EnglishPlatform
eas build --profile development --platform android
```

**Что произойдет:**
- ✅ Соберётся APK с Expo Dev Client
- ✅ API URL будет `http://212.67.11.39:3002/api`
- ✅ Можно подключаться к Metro для hot reload
- ⚠️ Занимает больше места (~100MB+)

### Вариант 3: Локальная сборка (Быстрее, но нужен Android SDK)

Если у вас установлен Android SDK, можно собрать локально:

```bash
cd c:/dev/EnglishPlatform
eas build --profile preview --platform android --local
```

**Требования:**
- Android Studio
- Java JDK 17
- Android SDK
- ~10GB свободного места

## После сборки

1. **Скачайте APK**
   - EAS Build покажет ссылку для скачивания
   - Или зайдите на https://expo.dev/accounts/[your-username]/projects/EnglishPlatform/builds

2. **Установите на телефон**
   - Скопируйте APK на телефон
   - Откройте APK файл
   - Разрешите установку из неизвестных источников
   - Установите приложение

3. **Протестируйте**
   - Приложение будет подключаться к `http://212.67.11.39:3002/api`
   - Все функции должны работать как на эмуляторе

## Проверка API URL и Отладка

В приложении теперь есть **Debug** таб в нижнем меню. Там вы увидите:
- Информацию о платформе и версии
- Текущую конфигурацию API URL
- Все логи запросов и ошибок в реальном времени

Чтобы проверить что происходит:
1. Откройте приложение на телефоне
2. Перейдите на таб **Debug** (иконка жука 🐛)
3. Посмотрите секцию "API Configuration" - там должен быть `http://212.67.11.39:3002/api`
4. Попробуйте сделать любой запрос (например, войти)
5. В секции "Application Logs" появятся детальные логи всех запросов

Если API URL неправильный или запросы не проходят - все детали будут видны в Debug табе!

## Troubleshooting

### "Build failed" ошибка
- Проверьте что все зависимости установлены: `npm install`
- Проверьте что нет синтаксических ошибок
- Посмотрите логи билда в веб-интерфейсе EAS

### APK не устанавливается
- Убедитесь что разрешена установка из неизвестных источников
- Проверьте что минимальная версия Android совместима (обычно Android 6.0+)

### Приложение не подключается к серверу
- Проверьте что сервер `212.67.11.39:3002` доступен
- Убедитесь что на сервере нет CORS ограничений для мобильных приложений
- Проверьте что используется `http://` а не `https://` (если сервер без SSL)

## Быстрый старт

Для быстрой сборки тестового APK выполните:

```bash
cd c:/dev/EnglishPlatform
eas build --profile preview --platform android
```

Дождитесь окончания сборки (~10-15 минут), скачайте APK и установите на телефон!

## Примечания

- **Preview билд** - лучший вариант для тестирования на реальном устройстве
- **Development билд** - нужен если хотите подключаться к Metro для разработки
- **Production билд** - для публикации в Google Play (создаёт AAB, не APK)

API URL уже настроен на ваш сервер в `eas.json`, поэтому дополнительная конфигурация не требуется!
