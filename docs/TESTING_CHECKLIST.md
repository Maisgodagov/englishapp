# Чеклист тестирования оптимизации видео

## 🎬 Подготовка к тестированию

- [ ] Запустить приложение (`npm start` или `npx expo start`)
- [ ] Открыть консоль для просмотра логов
- [ ] Подготовить блокнот для записи результатов

## 📱 Тест 1: Проверка базовой функциональности

### Шаги:
1. Откройте раздел "Видео" в приложении
2. Дождитесь загрузки первого видео
3. Просмотрите видео в течение 30 секунд

### Что проверить:
- [ ] Видео загружается и воспроизводится без ошибок
- [ ] Субтитры синхронизированы
- [ ] Счетчик трафика отображается в правом верхнем углу
- [ ] Кнопка паузы/воспроизведения работает
- [ ] Прогресс-бар отображается корректно

### Ожидаемые логи:
```
[VideoFeedItem abc12345] 🎬 Render: isActive=true, shouldLoad=true
[DataUsageTracker] 🆕 New content: abc12345
[DataUsageTracker] ▶️ Tracking enabled
[DataUsage abc12345] 🎥 Source loaded
[VideoFeedItem abc12345] ✅ Video loaded
```

## 📊 Тест 2: Измерение потребления трафика

### Шаги:
1. Сбросьте счетчик трафика (кнопка "Сбросить")
2. Просмотрите ровно 1 минуту видео (60 секунд)
3. Запишите значение счетчика трафика

### Что записать:
```
Качество видео: ___________ (проверьте в логах: resolution)
Битрейт: ___________ Kbps (проверьте: totalBitrateKbps)
Потребленный трафик за 1 мин: ___________ MB
```

### Ожидаемые значения:
- [ ] 360p: 5-8 MB/мин ✅
- [ ] 480p: 8-12 MB/мин ✅
- [ ] 720p: 12-18 MB/мин ✅
- [ ] Более 20 MB/мин ❌ (проблема!)

### Важные логи:
```
[DataUsage abc12345] 🎥 Source loaded
  totalBitrateKbps: ____
  estimatedMBPerMinute: ____

[DataUsage abc12345] 📦 Buffer update
  bufferAheadSeconds: ____ (должно быть 3-6)
  deltaKiloBytes: ____
```

## 🔄 Тест 3: Переключение между видео

### Шаги:
1. Сбросьте счетчик трафика
2. Свайпните вниз 5 раз (переключение на следующее видео)
3. На каждом видео подождите 2-3 секунды
4. Запишите итоговое значение счетчика

### Что записать:
```
5 переключений с просмотром 2-3 сек каждое:
Потребленный трафик: ___________ MB
```

### Ожидаемые значения:
- [ ] Менее 10 MB ✅ (отлично)
- [ ] 10-15 MB ✅ (хорошо)
- [ ] Более 20 MB ❌ (избыточная буферизация!)

### Что искать в логах:
```
[DataUsage abc12345] 📦 Buffer update
  bufferAheadSeconds: ____ (не должно превышать 6-7)

[VideoFeedItem abc12345] 👁️ shouldLoad changed: false
[DataUsageTracker] ⏸️ Tracking disabled
```

**Важно:** При переключении видео старый трекер должен отключаться!

## 📶 Тест 4: Адаптивное качество (HLS)

### Шаги:
1. Откройте видео
2. Симулируйте медленное соединение (если возможно)
3. Наблюдайте за переключениями качества в логах

### Что искать:
```
[DataUsage abc12345] 🔄 Bandwidth update ⬇️ DOWNGRADED
  oldBitrateKbps: 1800
  newBitrateKbps: 1000
  resolution: 854x480
  changePercent: -44.4%
```

### Проверить:
- [ ] При медленном интернете качество автоматически снижается
- [ ] При быстром интернете качество повышается (но не выше 720p из-за maxBitRate)
- [ ] Видео не зависает при переключениях качества
- [ ] Переключения логируются корректно

## ⏱️ Тест 5: Буферизация

### Шаги:
1. Откройте видео
2. Подождите 5 секунд без взаимодействия
3. Проверьте логи буферизации

### Что искать:
```
[VideoFeedItem abc12345] 📥 Buffer START
[VideoFeedItem abc12345] 📥 Buffer END

[DataUsage abc12345] 📦 Buffer update
  bufferedSeconds: ____
  currentTime: ____
  bufferAheadSeconds: ____ (критично!)
```

### Проверить:
- [ ] `bufferAheadSeconds` обычно в диапазоне 3-6 секунд ✅
- [ ] `bufferAheadSeconds` регулярно превышает 8 секунд ❌
- [ ] Буферизация не зависает на "Buffer START"

## 🔎 Тест 6: PhraseSearch (поиск фраз)

### Шаги:
1. Откройте раздел "Поиск фраз"
2. Введите фразу (например: "I am")
3. Нажмите "Найти"
4. Просмотрите 2-3 результата

### Что проверить:
- [ ] Видео фрагменты загружаются
- [ ] Воспроизведение начинается с правильного момента
- [ ] Видео останавливается в конце фрагмента
- [ ] Трекер работает для каждого фрагмента
- [ ] При переключении между карточками старые видео останавливаются

### Ожидаемые логи:
```
[PhraseSearch snippet123] ✅ Video loaded: {duration: 120, startSeconds: 45, endSeconds: 50}
[PhraseSearch snippet123] ⏹️ Reached end of snippet
[DataUsageTracker] ⏸️ Tracking disabled
```

## 🧪 Тест 7: Стресс-тест

### Шаги:
1. Сбросьте счетчик трафика
2. Быстро свайпните вниз 20 раз подряд (не останавливаясь)
3. Проверьте счетчик трафика

### Что записать:
```
20 быстрых переключений:
Потребленный трафик: ___________ MB
```

### Ожидаемые значения:
- [ ] Менее 30 MB ✅ (отлично!)
- [ ] 30-50 MB ⚠️ (приемлемо, но можно лучше)
- [ ] Более 50 MB ❌ (проблема - видео не успевают останавливаться)

### Что проверить в логах:
- [ ] Для каждого неактивного видео появляется `shouldLoad changed: false`
- [ ] Трекер отключается: `Tracking disabled`
- [ ] Нет утечек (одновременно активен только 1 трекер)

## 📝 Тест 8: Копирование логов

### Шаги:
1. Откройте видео и просмотрите 1 минуту
2. Скопируйте все логи из консоли
3. Сохраните их в файл `video_logs.txt`

### Команды для копирования:

**iOS Simulator:**
```bash
xcrun simctl spawn booted log stream --predicate 'eventMessage contains "DataUsage" OR eventMessage contains "VideoFeedItem"' > video_logs_ios.txt
```

**Android Emulator:**
```bash
adb logcat | grep -E "(DataUsage|VideoFeedItem|PhraseSearch)" > video_logs_android.txt
```

**Web (Chrome DevTools):**
- Откройте консоль (F12)
- Правый клик → Save as...

### Что должно быть в логах:
- [ ] `[DataUsageTracker]` сообщения о включении/отключении
- [ ] `[DataUsage]` сообщения о загрузке, буферизации, bandwidth
- [ ] `[VideoFeedItem]` сообщения о жизненном цикле
- [ ] Нет ошибок (❌ Video error)
- [ ] Нет warning'ов о memory leaks

## 🎯 Финальная проверка

### Общие вопросы:
- [ ] Видео воспроизводится плавно
- [ ] Нет явных лагов или зависаний
- [ ] Субтитры синхронизированы
- [ ] Переключение между видео работает быстро
- [ ] Счетчик трафика обновляется в реальном времени
- [ ] Приложение не крашится

### Производительность:
- [ ] Среднее потребление трафика соответствует ожиданиям (см. Тест 2)
- [ ] Буфер не превышает 6-7 секунд (см. Тест 5)
- [ ] При быстром переключении не загружается избыточный трафик (см. Тест 7)

### Качество видео:
- [ ] Максимальное разрешение не превышает 720p (из-за maxBitRate=2000000)
- [ ] При медленном интернете качество автоматически снижается
- [ ] При быстром интернете качество повышается до 720p

## 📤 Отправка результатов

После завершения всех тестов, соберите:

1. **Метрики трафика:**
   - Тест 2: ___ MB/мин
   - Тест 3: ___ MB за 5 переключений
   - Тест 7: ___ MB за 20 переключений

2. **Файлы логов:**
   - `video_logs_ios.txt` или `video_logs_android.txt`

3. **Скриншоты (по желанию):**
   - Счетчик трафика после 1 минуты просмотра
   - Пример логов в консоли

## 🔧 Что делать при проблемах

### Проблема: Видео не загружается

**Симптомы:**
```
[VideoFeedItem] ❌ Video error: {...}
```

**Решение:**
1. Проверьте URL видео в логах
2. Откройте URL в браузере (должен быть .m3u8 файл)
3. Проверьте доступность CDN
4. Проверьте CORS настройки

### Проблема: Высокий трафик (>20 MB/мин для 720p)

**Симптомы:**
```
bufferAheadSeconds: 10-15
estimatedMBPerMinute: 25+
```

**Решение:**
1. Убедитесь, что `maxBufferMs: 6000` применился:
   ```bash
   grep "maxBufferMs" src/features/video-learning/ui/VideoFeedItem.tsx
   ```
2. Проверьте, что трекер отключается при смене видео
3. Попробуйте уменьшить `maxBufferMs` до 4000

### Проблема: Трекер не работает

**Симптомы:**
- Нет логов `[DataUsage]`
- Счетчик трафика не обновляется

**Решение:**
1. Проверьте, что трекер подключен:
   ```typescript
   const dataUsageTracker = useVideoDataUsageTracker({
     enabled: shouldLoad,
     contentId: content.id,
   });
   ```
2. Проверьте, что обработчики вызываются:
   ```typescript
   onLoad={(data) => {
     dataUsageTracker.handleLoad(data);  // ✅
   }}
   ```

### Проблема: Частые переключения качества

**Симптомы:**
```
🔄 Bandwidth update ⬆️ UPGRADED
🔄 Bandwidth update ⬇️ DOWNGRADED (повторяется часто)
```

**Решение:**
1. Проверьте стабильность интернета
2. Увеличьте `minBufferMs` до 3000-3500
3. Проверьте нагрузку на CDN

---

**Удачи с тестированием! 🚀**

При возникновении вопросов или проблем, предоставьте:
- Описание проблемы
- Логи из консоли
- Результаты тестов из этого чеклиста
