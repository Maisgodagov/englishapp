# Миграция на HLS видео-поток

## Что изменилось?

Бэкенд теперь хранит видео в формате HLS (HTTP Live Streaming) вместо прямых MP4 файлов.

### Было (MP4)
```
videoUrl: "https://cdn.example.com/videos/video123.mp4"
```

### Стало (HLS)
```
videoUrl: "https://cdn.example.com/videos/abc123-def456/master.m3u8"
```

## Изменения в коде

### Старый способ (только для MP4)
```typescript
const player = useVideoPlayer(content.videoUrl, (player) => {
  player.loop = true;
  player.volume = 1.0;
});
```

### Новый способ (поддержка HLS)
```typescript
const player = useVideoPlayer(
  {
    uri: content.videoUrl,
    ...(content.videoUrl.includes('.m3u8') && { contentType: 'hls' as const }),
  },
  (player) => {
    player.loop = true;
    player.volume = 1.0;
  }
);
```

## Почему это нужно?

1. **Автоматическое качество**: плеер сам выбирает оптимальное разрешение (360p/480p/720p) в зависимости от скорости интернета
2. **Быстрый старт**: видео начинает воспроизводиться почти мгновенно, без загрузки всего файла
3. **Экономия трафика**: загружаются только нужные сегменты
4. **Стабильность**: при медленном интернете качество снизится автоматически, но видео продолжит воспроизводиться

## Обновленные компоненты

Все три компонента уже обновлены:

1. ✅ [VideoFeedItem.tsx](../src/features/video-learning/ui/VideoFeedItem.tsx) - лента видео
2. ✅ [VideoLearningSession.tsx](../src/features/video-learning/ui/VideoLearningSession.tsx) - сессии обучения
3. ✅ [PhraseSearch.tsx](../src/features/video-learning/ui/PhraseSearch.tsx) - поиск фраз

## Тестирование

### Как проверить, что всё работает?

1. Запустите приложение на любой платформе (iOS/Android/Web)
2. Откройте любое видео
3. Убедитесь, что:
   - ✅ Видео загружается и воспроизводится
   - ✅ Перемотка работает
   - ✅ Субтитры синхронизированы
   - ✅ Прогресс-бар работает корректно

### Проверка HLS в браузере

Откройте в браузере URL из `videoUrl`:
```
https://cdn.example.com/videos/abc123-def456/master.m3u8
```

Вы должны увидеть текстовый плейлист примерно такого вида:
```
#EXTM3U
#EXT-X-VERSION:3
#EXT-X-STREAM-INF:BANDWIDTH=2000000,RESOLUTION=1280x720
720p.m3u8
#EXT-X-STREAM-INF:BANDWIDTH=1000000,RESOLUTION=854x480
480p.m3u8
#EXT-X-STREAM-INF:BANDWIDTH=500000,RESOLUTION=640x360
360p.m3u8
```

## Что дальше?

Ничего! 🎉

Плеер `react-native-video` автоматически:
- Определяет HLS формат по расширению `.m3u8`
- Выбирает оптимальное разрешение
- Переключается между разрешениями при изменении скорости сети
- Буферизует сегменты для плавного воспроизведения

## Обратная совместимость

Код поддерживает как HLS (.m3u8), так и прямые MP4 ссылки:

```typescript
// Работает с HLS
videoUrl: "https://cdn.example.com/videos/abc123/master.m3u8"

// Работает с MP4 (если бэкенд вернет MP4)
videoUrl: "https://cdn.example.com/videos/video.mp4"
```

Условие `content.videoUrl.includes('.m3u8')` автоматически определяет формат.

## Troubleshooting

### Видео не воспроизводится

1. **Проверьте URL**: откройте его в браузере
2. **Проверьте CDN**: убедитесь, что сервер доступен
3. **Проверьте консоль**: ищите ошибки сети или плеера
4. **Проверьте CORS**: CDN должен разрешать запросы с вашего домена

### Видео тормозит

1. HLS автоматически снизит качество при медленном интернете
2. Проверьте скорость соединения
3. Убедитесь, что CDN оптимально размещен географически

### Вопросы?

Смотрите полную документацию в [HLS_VIDEO_SUPPORT.md](./HLS_VIDEO_SUPPORT.md)
