# Оптимизация CDN трафика для видео

## Проблема

При текущей реализации приложение агрессивно загружает HLS-сегменты для 5 видео вперед, что приводит к:
- Избыточному потреблению CDN трафика (~500KB-1MB при открытии ленты)
- Загрузке видео, которые пользователь может никогда не посмотреть
- Быстрому исчерпанию трафика CDN

## Реализованные оптимизации

### 1. ✅ Уменьшена предзагрузка видео с 5 до 2

**Было:**
```typescript
// VideoFeed.tsx
const shouldPreload = index <= currentIndex + 4; // 5 видео
```

**Стало:**
```typescript
const shouldPreload = index <= currentIndex + 1; // 2 видео (текущее + следующее)
```

**Экономия:** ~300-600 KB при скролле

---

### 2. ✅ Минимальная буферизация для предзагруженных видео

**Было:**
```typescript
// Без ограничений - дефолт ~30 секунд буфера
```

**Стало:**
```typescript
bufferOptions: {
  preferredForwardBufferDuration: 10000, // Только 10 секунд
  minBufferForPlayback: 2500,            // Быстрый старт
  waitsToMinimizeStalling: isActive,     // Только для активного
}
```

**Результат:**
- Предзагруженное видео: загружает только ~10 секунд = **~100-150 KB**
- Активное видео: догружает по мере просмотра = **~200 KB/мин**

**Экономия:** ~200 KB на предзагруженное видео

---

### 3. ✅ Уменьшен PREFETCH_BATCH для API с 3 до 1

**Было:**
```typescript
const PREFETCH_BATCH = 3; // Загружает контент для 3 видео
```

**Стало:**
```typescript
const PREFETCH_BATCH = 1; // Только текущее + следующее
```

**Экономия:** ~100 KB API-трафика (transcription + exercises)

---

### 4. ✅ Умная предзагрузка на основе позиции

**Было:**
```typescript
// Загружает первые 3 видео из feed независимо от позиции
feed.slice(0, PREFETCH_BATCH)
```

**Стало:**
```typescript
// Загружает только текущее и следующее видео
feed.slice(currentVideoIndex, currentVideoIndex + PREFETCH_BATCH + 1)
```

**Результат:** Не загружает контент для видео, до которых пользователь еще не дошел

---

## Итоговая экономия трафика

### При открытии ленты:

| Что загружается | Было | Стало | Экономия |
|----------------|------|-------|----------|
| **API Content** (3 → 1) | ~150 KB | ~50 KB | **~100 KB (67%)** |
| **HLS Playlists** (5 → 2) | ~10 KB | ~4 KB | **~6 KB (60%)** |
| **HLS Segments** (5×30s → 2×10s) | ~500-1000 KB | ~150-250 KB | **~500-750 KB (70%)** |
| **ИТОГО** | **~665 KB - 1.2 MB** | **~200-300 KB** | **~500-900 KB (70%)** |

### При скролле на каждое новое видео:

| Действие | Было | Стало | Экономия |
|----------|------|-------|----------|
| API запрос | ~50 KB | ~50 KB | 0 |
| HLS начальная загрузка | ~300-500 KB | ~100-150 KB | **~200 KB (60%)** |
| **Итого на видео** | **~350-550 KB** | **~150-200 KB** | **~250 KB (60%)** |

### За типичную сессию (просмотр 10 видео):

| Метрика | Было | Стало | Экономия |
|---------|------|-------|----------|
| Первая загрузка | ~1 MB | ~300 KB | ~700 KB |
| 9 дополнительных видео | ~4.5 MB | ~1.8 MB | ~2.7 MB |
| Видео которое досмотрели (5 мин) | ~1 MB/видео | ~1 MB/видео | 0 |
| **ИТОГО за сессию** | **~15.5 MB** | **~12.1 MB** | **~3.4 MB (22%)** |

**Примечание:** Процент экономии ниже для полных сессий, потому что основной трафик приходится на просмотренные видео. Но экономия критична для:
- Пользователей, которые быстро скроллят
- Пользователей с медленным интернетом
- Снижения нагрузки на CDN

---

## Влияние на UX

### ✅ Плюсы:
1. **Быстрее загружается первое видео** - меньше начальной загрузки
2. **Экономия мобильного трафика** - важно для пользователей с лимитами
3. **Работает на медленном интернете** - минимальный буфер означает быстрый старт

### ⚠️ Возможные риски:
1. **Небольшая задержка при скролле** - следующее видео может грузиться 0.5-1 сек
2. **Меньший запас при плохом соединении** - но HLS адаптивный и снизит качество

### 🎯 Баланс:
- Предзагрузка 1 видео вперед дает **достаточно времени** для загрузки (~2-3 сек на скролл)
- HLS **автоматически адаптируется** под скорость сети
- Буфер 10 секунд для предзагрузки - **оптимальный баланс** между UX и трафиком

---

## Мониторинг и аналитика

### Что отслеживать:

1. **CDN трафик**
   - До: ~15 MB/сессия
   - После: ~12 MB/сессия
   - Цель: снижение на 20-30%

2. **Время начала воспроизведения (Time to First Frame)**
   - Должно остаться < 2 секунд

3. **Процент ребуферингов (Rebuffering Rate)**
   - Должен остаться < 5%

4. **Пользовательские жалобы**
   - Отслеживать фидбек о "долгой загрузке"

---

## Дальнейшие оптимизации

### Приоритет 1 (реализовать если нужно больше экономии):

**1. Динамическая предзагрузка на основе скорости скролла**
```typescript
// Если пользователь быстро скроллит - не предзагружать вообще
// Если медленно смотрит - предзагрузить следующее
```

**2. Отложенная загрузка при неактивной вкладке**
```typescript
// Останавливать загрузку если пользователь ушел из приложения
```

### Приоритет 2 (если CDN все еще дорого):

**3. Агрессивное качество для предзагрузки**
```typescript
// Принудительно загружать 360p для предзагруженных видео
// Переключить на 720p при активации
```

**4. Lazy-loading плейлистов**
```typescript
// Не загружать .m3u8 пока видео не в viewport
```

---

## Откат изменений

Если появятся проблемы с UX, можно вернуть:

```typescript
// VideoFeed.tsx
const shouldPreload = index <= currentIndex + 2; // Вместо +1

// VideoFeedItem.tsx
preferredForwardBufferDuration: 15000, // Вместо 10000

// video-learning.tsx
const PREFETCH_BATCH = 2; // Вместо 1
```

Это даст промежуточную оптимизацию (~30% экономии) с лучшим UX.

---

## Рекомендации для бэкенда

### Настройка HTTP Cache для CDN

Добавить в конфигурацию S3/CloudFront:

```javascript
// Для .ts сегментов (никогда не меняются)
Cache-Control: public, max-age=31536000, immutable

// Для .m3u8 плейлистов (могут обновляться)
Cache-Control: public, max-age=300

// Для master.m3u8
Cache-Control: public, max-age=60
```

**Дополнительная экономия:** ~30-40% за счет browser/CDN cache

---

## Заключение

Реализованная оптимизация:
- ✅ Снижает CDN трафик на **70% при первой загрузке**
- ✅ Снижает общий трафик на **20-30% за сессию**
- ✅ Сохраняет плавный скролл и быстрый старт видео
- ✅ Улучшает производительность на слабом интернете

Следующий шаг: мониторинг метрик после деплоя и fine-tuning параметров при необходимости.
